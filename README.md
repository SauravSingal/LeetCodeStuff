Arrays.sort(numbers); //Sort in ascending order
Arrays.sort(numbers, Collection.reverseOrder()); //Sort in descending order

Array  .length  array.length  Number of elements in the array
String .length() srt.length() Number of characters in the string
